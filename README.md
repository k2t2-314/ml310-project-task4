This folder is used to deploy Tasks 1–3 on a virtual machine using Docker.
After cloning the repository, please rename Dockerfile.dockerfile to Dockerfile with the following command:
```
mv Dockerfile.dockerfile Dockerfile
```
